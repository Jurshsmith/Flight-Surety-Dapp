Write rough implementation -> Write tests -> Complete implementation ??

```
    truffleAssert.eventEmitted(airlineRegistration, 'Logger', (ev) => {
      console.log({ ev });
      return false
    });

    // Snippet for debugging right from the test

```
